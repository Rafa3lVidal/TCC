<Page
    x:Class="NFC_King.Pages.Email"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NFC_King.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer Padding="{x:Bind Frame.Padding, Mode=OneWay}">
            <StackPanel>
                <TextBox x:Name="TxtBoxEndEmail" TextWrapping="Wrap" Text="digite o email do  destinatário..." Margin="0,9,0,9" />
                <TextBox x:Name="TxtBoxAssuntoEmail" TextWrapping="Wrap" Text="digite o assunto..." Margin="0,0,0,9" />
                <TextBox x:Name="TxtBoxMsgEmail" TextWrapping="Wrap" Text="digite sua mensagem..." Margin="0,0,0,9" />
                <TextBlock x:Name="TxtStatus" TextWrapping="Wrap" Text="" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20"/>

                <Button x:Name="BtnGravaURI" Content="Gravar" HorizontalAlignment="Stretch" VerticalAlignment="Center"  Click="BtnGravaURI_Click" Margin="0,23,0,-1" Height="75" FontSize="24" Padding="8,4" ToolTipService.ToolTip="Grava a informação selecionada na tag." RenderTransformOrigin="0.5,0.5" />
                
            </StackPanel>
            
        </ScrollViewer>
       
    </Grid>
</Page>
