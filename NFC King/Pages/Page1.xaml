<Page
    x:Class="NFC_King.Pages.Page1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NFC_King.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer Padding="{x:Bind Frame.Padding, Mode=OneWay}">
            <StackPanel>


                <Button  x:Name="BtnLigacao" Content="" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontFamily="Segoe MDL2 Assets" Margin="0,20,0,0" Height="49" FontSize="20" Click="BtnLigacao_Click"/>
                <Button  x:Name="BtnSms" Content="" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontFamily="Segoe MDL2 Assets" Margin="0,5,0,0" Height="50" FontSize="20" Click="BtnSms_Click"/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
